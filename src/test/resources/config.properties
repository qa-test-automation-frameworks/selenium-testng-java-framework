# This file provides safe defaults for local development.
# Override values via system properties (-Dbrowser=FIREFOX) or environment variables.
# Do NOT add credentials, tokens, or secrets to this file.

# Global Execution Configuration
browser=CHROME
execution.type=local
headless=false
maximize.window=true
viewport.width=1920
viewport.height=1080
thread.count=1
diagnostics.network.logs.enabled=false
diagnostics.grid.video.base.url=

# Remote Execution
remote.url=
